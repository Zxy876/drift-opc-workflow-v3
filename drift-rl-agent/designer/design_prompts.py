"""
LLM Prompt 模板库 — DesignerAgent 使用的所有 Prompt
"""

# ─── 关卡改进 Prompt ───────────────────────────────────────
LEVEL_IMPROVEMENT_PROMPT = """你是 Drift MC 关卡设计师。根据 AI 玩家的测试数据改进关卡设计。

## 当前关卡设计
{current_design}

## AI 玩家测试数据（{total_episodes} 局）
- 通关率: {completion_rate:.0%}
- 平均完成时间: {avg_time:.0f}s
- 平均死亡次数: {avg_deaths:.1f}
- /easy 使用率: {easy_usage_rate:.0%}
- 主要死因: {death_causes}
- 卡点位置: {stuck_points}

## 目标
- 目标难度: D{target_difficulty}
- 目标通关率: 60%-80%（Flow Zone）
- 当前通关率偏{direction}

## Drift 解析器支持的语法
- 触发器: "收集 N 个 <物品>", "打败 N 个 <怪物>", "到达 <地点>", "在 N 秒内..."
- NPC: "NPC <名字> 在 <位置> <行为>"
- 资源: 宝石, 钥匙, 符文, 珍珠 等
- 地形: 森林, 沙漠, 浮空岛, 地下洞穴, 海底神殿 等
- 难度标记: "难度 D1/D2/D3/D4/D5"
- 时间限制: "在 N 秒内完成"
- Boss: "Boss <名字> 血量 N 攻击力 N"

## 改进规则
1. 如果通关率 < 60%: 减少怪物数量/延长时间限制/增加提示/增加补给
2. 如果通关率 > 80%: 增加挑战/缩短时间限制/增加机关/增加 Boss 难度
3. 如果 /easy 使用率 > 50%: 基础难度明显过高，需要大幅降低
4. 保持叙事连贯性，不要完全重写，在原设计基础上微调
5. 每次最多改动 2-3 个参数，避免震荡

## 输出格式
严格输出 JSON：
{{
  "design_text": "改进后的完整设计描述文本（中文）",
  "difficulty": 数字(1-5),
  "reasoning": "改进理由（50字以内）",
  "changes": ["改动1", "改动2"]
}}
"""

# ─── 新关卡生成 Prompt ──────────────────────────────────────
NEW_LEVEL_PROMPT = """你是 Drift MC 关卡设计师。根据要求生成一个全新的关卡设计。

## 要求
- 目标难度: D{target_difficulty}
- 主题: {theme}
- 关卡类型: {level_type}

## Drift 解析器语法
- 触发器: "收集 N 个 <物品>", "打败 N 个 <怪物>", "到达 <地点>"
- NPC: "NPC <名字> 在 <位置> <行为>"
- 时间限制: "在 N 秒内完成"

## 难度参考
- D1: 1-2 个简单触发器，无时间限制，无怪物
- D2: 2-3 个触发器，可能有弱怪物
- D3: 3-5 个触发器，有怪物和简单机关
- D4: 5-8 个触发器，多波怪物，时间压力
- D5: 8+ 个触发器，Boss 战，复杂机关，严格时间限制

## 输出格式
严格输出 JSON：
{{
  "design_text": "完整的关卡设计描述（中文）",
  "difficulty": 数字(1-5),
  "title": "关卡标题"
}}
"""
